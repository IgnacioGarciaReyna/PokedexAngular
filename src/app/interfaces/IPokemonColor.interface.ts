export interface IPokemonColor {
  type: string;
  color: string;
}

export interface IEggColor {
  eggType: string;
  color: string;
}

export interface IPokedexColor {
  name: string;
  primaryColor: string;
  secondaryColor: string;
}
